1.  Implement the following set methods:
        set_dynamics()
        set_marks()
        set_markup()
        set_tempo()

2.  Implement a method to slur leaves by divisions.
    Implement general methods to span leaves by divisions.

3.  Change set_note_head_color() to set_leaf_color().

4.  Hoist ...
        leaves = []
        for target_select_expression in self.target_select_expression_inventory:
            iterable_payload_expression = target_select_expression.evaluate_against_score(score)
            leaves.extend(iterable_payload_expression.payload)
    ... to LeafSetExpression.

5.  Change set_pitches() to set_pitch().
