1.  Edit instrument transposition.

2.  Edit instrument clefs.

3.  Remove extra appearance of B-flat clarinet from clarinetist config menu.

4.  Change Instrument._default_performer_names 
    to house (full, abbreviation) pairs.

5.  Use performer abbreviations to populate performer selection menu.

6.  Make user input getter defaults work like menu defaults.
    Implement default_value on user input getter.

7.  Fix ScoreManagerObject.handle_user_input_with_default() default handling.

8.  Define range inventories for all scores.

9.  Make user input handler creation work.

10. Find a way dynamcially to present default indicator 
    with default menu entries.

11. Distinguish positional editor parameters from keyword editor parameters.

12. Implement something to tour all materials everywhere 
    and examine in sequence.

13. Currently MaterialPackageMaker inherits from MaterialPackageProxy.
    Maybe MaterialPackageMaker should *have* a MaterialPackageProxy instead.
    This would clean up the inheritance schema somewhat.

14. Audit the UI of the entire materials management system by hand.

15. Add a scores/test directory and check it into the scores repository.
    Then add a scores/test/test_scores_import_scores.py file.
    The file will test 'import red_example_score', 
    'import green_example_score', and so on.
    This will be a very high-level test to make sure 
    that entire score packages remain importable.

16. Implement example_score_i.music.materials.divisions to import 
    example_score_i.music.materials.time_signatures.
    This will implement an intrapackage material import.
    Implement example_score_ii.music.materials.divisions to 
    import example_score_i.music.materials.time_signatures.
    This will implement an interpackage material import.

17. Remove ModuleProxy.unimport() entirely.

18. Replace all _read_only_attributes.py and _public_attributes.py 
    test files with doctests.

19. Sweep inline scoremanagertools TODO items.

20. Author ScorePackageProxy docstrings.

21. Add PackageystemAssetProxy to superclass ModuleProxy and PackageProxy.

22. Create a PackagesystemAssetProxy to parallel PacakagesystemAssetWrangler.

23. Harmonize user input strings with method names in 
    MaterialPackageProxy.user_input_to_action dictionary.

24. Author StylesheetFileWrangler.interactively_remove_assets() test.
    Will involve making user_input='... rm ...' work with named arguments 
    instead of just numeric arguments.

25. Make MaterialPackageMaker classes stop creating materials in 
    tools.scoremanagertools.materialpackages.
    Those classes should create material packages in the user asset 
    library material packages directory.
    Or in user score packages. But no material packages should ever 
    be created in the built-in asset library material packages directory.

26. Implement material methods to move material packages 
    from one location to another.

27. Find a way to include tags management in hidden menu 
    of every package proxy.

28. Rename all tags.py files to __tags__.py instead.

29. Flatten all score_package/music directories.
    So instead of score_package/music/material_packages,
    score_package/music/segments, score_package/music/specifiers,
    score_package/music/stylesheets, etc. we'll have
    score_package/material_packages, score_package/segments,
    score_package/specifiers, score_package/stylesheets, etc.
    instead.

30. Encapsulate the several dozen UserInputGetter.append_*() methods
    in a separate mix-in class.

31. Install supertab.

32. Cache tags.

33. Change fold line indicator to shown line count only.
