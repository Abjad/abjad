from experimental.requesttools.MaterialRequest import MaterialRequest


class RhythmRequest(MaterialRequest):
    r'''.. versionadded:: 1.0

    ::

        >>> from experimental import *

    Return rhythm for `selector` in `context_name`.

    Apply any of `index`, `count`, `reverse`, `rotation`, `callback` that are not none.

    The purpose of a rhythm request is to function as the source of a setting.
    '''

    pass
