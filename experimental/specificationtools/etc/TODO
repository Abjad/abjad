1. Make divisions partitionable.

2. Migrate ScoreSpecification and SegmentSpecification interpreter code to new
   interpretationtools.ScoreInterpreter class.

3. Make it so that SegmentSpecification can say "import selectortools" globally.
   So specificationtools will depend on selectortools. And not the other way around.
   Same with settingtools and timespantools.

4. Extend DivisionList initializer with string handling.
