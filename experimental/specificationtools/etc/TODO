1. Change 'from experimental.specificationtools.TemporalScope import TemporalScope'
   to 'from experimental.specificationtools.TemporalScope.TemporalScope import TemporalScope'

2. Make Setting inherit from Directive. Or vice versa. Or make both inherit from an ABC.

3. Implement RetrievalRequest as base class for different retrieval requests classes.
