1.  Change the order of UninterpretedDvisionCommand arguments from ...
        duration, start_offset, stop_offset
    ... to ...
        start_offset, stop_offset, duration
    ... instead.

2. Make divisions (ie, nonreduced fractions) partitionable.
