1. Migrate ScoreSpecification and SegmentSpecification interpreter code to new
   interpretationtools.ScoreInterpreter class.

2. See if test_SegmentSpecification_select_ratio_of_background_measures.py works.

3. Make divisions (ie, nonreduced fractions) partitionable.
