1.  Make definition.py a mandatory part of every material package.

2.  Make (m 1 da b) stop prompting to write definition.py if definition.py
    has not changed. (m 1 da b) should continue to prompt to write
    definition.py if definition.py has changed during editing.

3.  Open __metadata__.py in editable mode; do not open read-only.
    Do continue to open output.py read-only.

4.  Unskip all skipped tests.

5.  Reinstate command concatenation.

6.  Implement !-suffixation.

7.  Reinstate autoadvance.

8.  Implement TestManager.compare_lys().

9.  Integrate diff-pdf.

10. Integrate candidacy protocol.

11. Clean up InstrumentInventory.
    Clean up PerformerInventory.

12. Remove (vnew). Remove (vrm). Remove (vren).
    Use (vae) instead.

13. Replace (vap) and (vcl) with (vs).

14. Implement 'views - write' (vw).
    Implement 'views - check' (vc).

15. Implement '__metadata__.py - check' (mdc).

16. Implement '__metadata__.py - autoedit' (mdae).

17. Change (?) to (??).

18. Fix double score name printing that follows (ck* y y).

19. Make (**) work.

20. Change (h) to (S).
    Implement (U), (D), (M), (G), (Y).

21. Audit (**).

22. Audit all star commands.

23. Audit material interface by hand.
    Audit segment interface by hand.

24. Audit all public methods for py.test files.

25. Answer all inline TODOs.

26. Streamline all 39 classfiles.